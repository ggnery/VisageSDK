from config.base_config import BaseConfig


class BackboneConfig(BaseConfig):
    """Backbone config — all keys come from YAML via attribute access.

    Required YAML keys: input_size, embedding_size, device.
    """

    pass
